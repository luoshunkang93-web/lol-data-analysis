name: Daily Data Update 🤖

# 触发条件：什么时候运行？
on:
  schedule:
    # 每天 UTC 时间凌晨 0 点 (北京时间早上 8 点) 运行
    - cron: '0 0 * * *'
  workflow_dispatch: # 允许你手动点击按钮运行 (方便测试)

jobs:
  update_data:
    runs-on: ubuntu-latest # 使用最新的 Linux 系统

    steps:
    # 1. 把代码拉取下来
    - name: Checkout code
      uses: actions/checkout@v3

    # 2. 安装 Python
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    # 3. 安装依赖库 (根据 requirements.txt)
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    # 4. 运行你的爬虫脚本！
    - name: Run Update Script
      env: # <--- 新增部分
        DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
      run: python update_db.py

    # 5. 把更新后的数据库存回仓库 (Commit & Push)
    - name: Commit and push changes
      run: |
        git config --global user.name "GitHub Action Bot"
        git config --global user.email "actions@github.com"
        git add lol_analysis.db
        # 如果有变化才提交，没变化就不提交
        git commit -m "🚨 Auto-update database data" || exit 0
        git pull --rebase origin main  # <--- 🆕 加这行！(意思是：提交前先拉取最新代码)
        git push